services:
  redis:
    image: redis:7-alpine
    container_name: edgehub-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - edgehub_redis_data:/data

volumes:
  edgehub_redis_data:
